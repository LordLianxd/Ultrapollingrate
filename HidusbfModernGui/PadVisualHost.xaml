<UserControl x:Class="HidusbfModernGui.PadVisualHost"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:HidusbfModernGui">
    <!-- Decide en runtime que dibuja el mando: Vector (PadVisual) es el que se ve por
         defecto y cuando no hay skin valido; Skinned (SkinnedPadVisual) se muestra cuando
         ReloadSkin() encuentra y carga un skin en %APPDATA%\UltraPolling\skins\. Los dos
         estan superpuestos en el mismo Grid, uno Collapsed y el otro Visible. -->
    <Grid>
        <local:PadVisual x:Name="Vector"/>
        <local:SkinnedPadVisual x:Name="Skinned" Visibility="Collapsed"/>
    </Grid>
</UserControl>
